---
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
import { ArrowRight, Sparkles } from "lucide-react";
import GridPattern from "@/components/ui/GridPattern.astro";
---

<section
  id="hero"
  class="relative min-h-[90vh] flex items-center justify-center overflow-hidden pt-20"
>
  {/* Modern Gradient Background - Transparent to show Global Layout */}
  <div class="absolute inset-0 w-full h-full bg-transparent">
    {/* Local orbs removed to use global ones */}
  </div>

  {/* Grid Pattern Overlay */}
  <GridPattern />

  <div
    class="container relative z-10 flex flex-col items-center text-center max-w-4xl px-4 pt-2"
  >
    <div
      class="inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/10 px-4 py-1.5 text-sm font-medium text-primary mb-8 backdrop-blur-sm animate-in fade-in slide-in-from-bottom-4 duration-500"
    >
      <Sparkles className="size-3.5" />
      <span>Automatización e Inteligencia Artificial</span>
    </div>

    <h1
      class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-black tracking-tight mb-8 text-foreground leading-[1.1] animate-in fade-in slide-in-from-bottom-6 duration-700 delay-100"
    >
      Impulsa tu negocio con <span class="title-outline-purple"
        >Inteligencia Artificial</span
      >
    </h1>

    <p
      class="text-lg md:text-xl text-muted-foreground mb-10 max-w-2xl leading-relaxed animate-in fade-in slide-in-from-bottom-8 duration-700 delay-200"
    >
      Transformamos tareas manuales en sistemas autónomos eficientes. Recupera
      tu tiempo y escala tu empresa con tecnología de vanguardia.
    </p>

    <div
      class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto animate-in fade-in slide-in-from-bottom-10 duration-700 delay-300"
    >
      <a
        href="#contact"
        class={cn(
          buttonVariants({ variant: "default", size: "lg" }),
          "h-14 px-8 text-lg rounded-full bg-primary hover:bg-primary/90 text-primary-foreground shadow-glow transition-all hover:scale-105 hover:shadow-[0_0_30px_rgba(94,23,235,0.5)]",
        )}
      >
        Empezar ahora
        <ArrowRight className="ml-2 size-5" />
      </a>
      <a
        href="#case-studies"
        class={cn(
          buttonVariants({ variant: "outline", size: "lg" }),
          "h-14 px-8 text-lg rounded-full border-border text-foreground hover:bg-secondary hover:text-secondary-foreground transition-all hover:scale-105 backdrop-blur-sm",
        )}
      >
        Ver casos de éxito
      </a>
    </div>

    {/* Tech Stack / Social Proof Strip (Optional but adds credibility) */}
    <div
      class="mt-20 pt-8 border-t border-white/5 w-full animate-in fade-in slide-in-from-bottom-12 duration-1000 delay-500"
    >
      <p
        class="text-xs text-muted-foreground uppercase tracking-widest mb-6 font-semibold"
      >
        Potenciando empresas con tecnología de punta
      </p>
      <div
        class="flex justify-center gap-8 opacity-50 grayscale hover:grayscale-0 transition-all duration-500"
      >
      </div>
    </div>
  </div>
</section>
