---
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
---

<section
  id="hero"
  class="relative h-screen flex items-center justify-center overflow-hidden bg-[url('https://via.placeholder.com/1920x1080?text=AI+Background')] bg-cover bg-center before:content-[''] before:absolute before:inset-0 before:bg-gradient-to-b before:from-black/70 before:to-black/90"
>
  <div
    class="container relative z-10 flex flex-col items-center text-center max-w-[800px]"
  >
    <div
      class="inline-flex items-center rounded-full border border-primary/30 px-3 py-1 text-sm font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 bg-primary/10 text-primary mb-8"
    >
      Automatización e Inteligencia Artificial
    </div>

    <h1
      class="text-4xl sm:text-5xl md:text-6xl font-bold tracking-tight mb-6 leading-[1.2]"
    >
      Automatización e Inteligencia Artificial para tu Negocio
    </h1>

    <p class="text-lg md:text-xl text-white/90 mb-10 leading-relaxed">
      Transformamos tareas repetitivas y procesos manuales en sistemas
      inteligentes y eficientes 24/7.
    </p>

    <div class="flex flex-col sm:flex-row gap-4">
      <a
        href="#contact"
        class={cn(
          buttonVariants({ variant: "default", size: "lg" }),
          "inline-flex text-xl px-10 py-8 h-auto hover:scale-105 transition-all duration-300 rounded-full shadow-2xl shadow-primary/40 ring-2 ring-primary/20 ring-offset-2 ring-offset-background",
        )}
      >
        Cuéntanos tu proyecto
      </a>
    </div>
  </div>
</section>
