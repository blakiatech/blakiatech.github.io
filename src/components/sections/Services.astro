---
import { services } from "@/data/services"
---

<section id="services" class="py-24 bg-canvas relative overflow-hidden">
  {/* Background glow */}
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[600px] bg-primary/10 blur-[120px] rounded-full -z-10"
  >
  </div>

  <div class="container px-4">
    <div class="text-center mb-16 relative">
      <h2
        class="text-4xl font-bold tracking-tight md:text-5xl mb-12 relative inline-block after:content-[''] after:absolute after:-bottom-4 after:left-1/2 after:-translate-x-1/2 after:w-[120px] after:h-[3px] after:bg-primary"
      >
        Lo que hacemos por tu negocio
      </h2>
      <p class="text-muted-foreground max-w-2xl mx-auto text-lg">
        Soluciones diseñadas para escalar tu operación sin aumentar tus costes
        fijos.
      </p>
    </div>

    {/* Single Glass Dashboard Card */}
    <div class="max-w-5xl mx-auto">
      <div
        class="bg-surface/40 backdrop-blur-md border border-white/5 rounded-3xl p-8 md:p-12 shadow-2xl shadow-black/50"
      >
        <div class="grid grid-cols-1 md:grid-cols-2 gap-x-12 gap-y-10">
          {
            services.map((service) => (
              <div class="flex gap-5 items-start group">
                <div class="shrink-0 p-3 rounded-xl bg-primary/20 text-primary border border-primary/10 group-hover:bg-primary group-hover:text-primary-foreground group-hover:border-primary/50 transition-all duration-500">
                  <service.icon className="size-6" strokeWidth={1.5} />
                </div>
                <div class="space-y-1">
                  <h3 class="text-lg font-bold text-foreground group-hover:text-primary transition-colors">
                    {service.title}
                  </h3>
                  <p class="text-muted-foreground text-sm leading-relaxed group-hover:text-foreground transition-colors">
                    {service.description}
                  </p>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</section>
