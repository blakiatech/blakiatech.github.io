---
import { benefits } from "@/data/benefits"
---

<section
  id="benefits"
  class="py-32 relative overflow-hidden bg-background dark:bg-zinc-950 text-foreground dark:text-white transition-colors duration-300"
>
  

  <div class="container relative z-10 px-4">
    <div class="flex flex-col lg:flex-row items-center justify-between gap-16">
      {/* Title Section */}
      <div class="lg:w-1/3 text-center lg:text-left space-y-6">
        <h2
          class="text-3xl md:text-4xl font-black tracking-tight text-foreground dark:text-white"
        >
          <span class="bg-clip-text text-transparent bg-gradient-to-r from-primary via-purple-600 to-purple-400 dark:via-purple-400">Beneficios Clave</span>
        </h2>
        <div class="h-1 w-20 bg-muted dark:bg-white/10 rounded-full mx-auto lg:mx-0 relative overflow-hidden">
            <div class="absolute inset-y-0 left-0 w-1/2 bg-secondary rounded-full animate-slide-right shadow-[0_0_10px_rgba(238,255,6,0.5)]"></div>
        </div>
        <p class="text-muted-foreground dark:text-zinc-400 text-lg max-w-md mx-auto lg:mx-0 leading-relaxed">
          Transforma tu operativa diaria con soluciones que generan valor real y medible desde el primer día.
        </p>
      </div>

      {/* Connected Flow Steps */}
      <div class="lg:w-2/3 grid grid-cols-1 md:grid-cols-3 gap-12 relative mt-10 lg:mt-0">
        
        {
          benefits.map((benefit, index) => (
            <div class="relative flex flex-col items-center text-center group">
              <div class="mb-8 relative">
                {/* Glow Effect */}
                <div class="absolute inset-0 bg-primary/20 blur-2xl rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                
                <div class="relative w-24 h-24 rounded-2xl bg-background dark:bg-zinc-900 border border-border dark:border-white/10 flex items-center justify-center text-foreground dark:text-white group-hover:text-primary dark:group-hover:text-secondary group-hover:border-primary dark:group-hover:border-secondary/50 transition-all duration-300 shadow-lg dark:shadow-xl group-hover:shadow-[0_0_30px_rgba(94,23,235,0.2)] group-hover:-translate-y-2 overflow-hidden">
                  <div class="absolute inset-0 bg-gradient-to-br from-primary/5 dark:from-white/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
                  <benefit.icon size={32} strokeWidth={1.5} className="relative z-10" />
                </div>
                
                {/* Step Number Badge */}
                <div class="absolute -top-3 -right-3 w-8 h-8 rounded-full bg-background dark:bg-zinc-950 border border-border dark:border-white/10 flex items-center justify-center text-xs font-mono font-bold text-muted-foreground dark:text-zinc-500 group-hover:text-primary dark:group-hover:text-secondary group-hover:border-primary dark:group-hover:border-secondary transition-colors shadow-sm">
                  {index + 1}
                </div>
              </div>

              <h3 class="text-xl font-bold mb-3 text-foreground dark:text-white group-hover:text-primary dark:group-hover:text-secondary transition-colors">
                {benefit.title}
              </h3>
              <p class="text-muted-foreground dark:text-zinc-400 text-sm leading-relaxed px-2 group-hover:text-foreground/80 dark:group-hover:text-zinc-300 transition-colors">
                {benefit.description}
              </p>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
@keyframes slide-right {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(200%); }
}
.animate-slide-right {
    animation: slide-right 2s infinite linear;
}
</style>