---
import { SocialLinks } from "./SocialLinks";

const QUICK_LINKS = [
  { name: "Beneficios", href: "/#benefits" },
  { name: "Servicios", href: "/#services" },
  { name: "Prensa", href: "/#press" },
  { name: "Contacto", href: "/contacto" },
];

const LEGAL_LINKS = [
  { name: "Política de Privacidad", href: "/privacy" },
  { name: "Política de Cookies", href: "/privacy" },
  { name: "Aviso Legal", href: "/privacy" },
];
---

<footer
  class="relative border-t border-border bg-muted/20 pt-24 pb-12 overflow-hidden"
>
  <!-- Decorative Elements -->
  <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
    <div
      class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full max-w-7xl opacity-40"
    >
      <div
        class="absolute top-[20%] left-[5%] w-125 h-125 bg-primary/5 rounded-full blur-[100px]"
      >
      </div>
      <div
        class="absolute bottom-[10%] right-[5%] w-100 h-100 bg-secondary/10 rounded-full blur-[80px]"
      >
      </div>
    </div>
  </div>

  <div class="container relative z-10 px-4 mx-auto">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-8 mb-20">
      <!-- Brand Column -->
      <div class="lg:col-span-5 space-y-8">
        <div class="space-y-4">
          <a
            href="/"
            class="flex items-center space-x-2 hover:opacity-80 transition-opacity"
          >
            <img
              src="/logo_blakIA_light.png"
              alt="BlakIA Logo"
              class="h-10 w-auto object-contain rounded-sm dark:hidden"
            />
            <img
              src="/logo_blakIA_dark.png"
              alt="BlakIA Logo"
              class="h-10 w-auto object-contain rounded-sm hidden dark:block"
            />
          </a>
          <p
            class="text-muted-foreground text-base leading-relaxed max-w-md text-balance"
          >
            Impulsamos el futuro de tu negocio integrando soluciones de
            <span class="text-foreground font-medium"
              >Inteligencia Artificial</span
            >
            que optimizan, automatizan y escalan tus operaciones.
          </p>
        </div>

        <!-- Partner Cards Container -->
        <div class="flex flex-wrap items-center gap-2">
          <div
            class="group/card bg-black p-2 rounded-xl border border-transparent hover:border-primary/20 transition-all duration-300 flex items-center justify-center shadow-sm hover:shadow-lg hover:-translate-y-1"
          >
            <img
              src="/el-patio-completo-logo-negativo.svg"
              alt="El Patio"
              class="h-full w-52 object-contain p-2"
            />
          </div>

          <div
            class="group/card bg-white p-2 rounded-xl border border-border/50 hover:border-primary/20 transition-all duration-300 flex items-center justify-center shadow-sm hover:shadow-lg hover:-translate-y-1"
          >
            <img
              src="/IMDEEC-Pegatinas-2025-Autoempleo-L1.jpg"
              alt="IMDEEC"
              class="h-full w-52 object-contain rounded-lg"
            />
          </div>
        </div>
      </div>

      <!-- Spacer -->
      <div class="hidden lg:block lg:col-span-1"></div>

      <!-- Navigation Columns -->
      <div class="lg:col-span-6 grid grid-cols-2 sm:grid-cols-2 gap-10">
        <div class="space-y-6">
          <h3 class="text-lg font-bold text-foreground relative inline-block">
            Explorar
            <span
              class="absolute -bottom-2 left-0 w-8 h-1 bg-primary rounded-full"
            ></span>
          </h3>
          <ul class="space-y-4">
            {
              QUICK_LINKS.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-muted-foreground hover:text-primary transition-colors text-[15px] hover:translate-x-1 inline-block duration-200"
                  >
                    {link.name}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="space-y-6">
          <h3 class="text-lg font-bold text-foreground relative inline-block">
            Legal
            <span
              class="absolute -bottom-2 left-0 w-8 h-1 bg-secondary rounded-full"
            ></span>
          </h3>
          <ul class="space-y-4">
            {
              LEGAL_LINKS.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-muted-foreground hover:text-primary transition-colors text-[15px] hover:translate-x-1 inline-block duration-200"
                  >
                    {link.name}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>

    <!-- Footer Bottom -->
    <div
      class="border-t border-border pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-muted-foreground"
    >
      <p class="flex items-center gap-1">
        &copy; {new Date().getFullYear()}
        <span class="font-bold text-foreground">BlakIA</span>. Todos los
        derechos reservados.
      </p>
      <div>
        <SocialLinks />
      </div>
    </div>
  </div>
</footer>
