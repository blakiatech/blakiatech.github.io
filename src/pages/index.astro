---
import PageLayout from "@/layouts/PageLayout.astro";
import Hero from "@/components/sections/Hero.astro";
import Services from "@/components/sections/Services.astro";
import Benefits from "@/components/sections/Benefits.astro";
import { MediaMentions } from "@/components/sections/MediaMentions";
import { ArrowRight } from "lucide-react";

const base = import.meta.env.BASE_URL;
---

<PageLayout>
  <Hero />
  <Benefits />
  <Services />
  <MediaMentions client:visible />
  
  {/* Final CTA Section replacing inline Contact */}
  <section class="py-32 relative overflow-hidden bg-background dark:bg-zinc-950 transition-colors duration-300">
    <div class="absolute inset-0 opacity-[0.03] dark:opacity-[0.05] mix-blend-overlay" style="background-image: url(&quot;data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E&quot;);"></div>
    <div class="container mx-auto px-4 relative z-10 text-center">
      <div class="max-w-4xl mx-auto space-y-8 p-12 rounded-[2.5rem] bg-white dark:bg-zinc-900/50 border border-border/40 dark:border-white/10 backdrop-blur-sm relative overflow-hidden group hover:border-secondary/30 transition-all duration-500 shadow-xl dark:shadow-none">
        <div class="absolute inset-0 bg-gradient-to-b from-primary/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        <h2 class="text-4xl md:text-5xl font-black tracking-tighter text-foreground dark:text-white relative z-10">
          ¿Listo para <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-purple-600 to-purple-400 dark:via-purple-400">transformar</span> tu negocio?
        </h2>
        <p class="text-xl text-muted-foreground dark:text-zinc-400 max-w-2xl mx-auto relative z-10">
          Deja que la inteligencia artificial se encargue de lo repetitivo mientras tú te enfocas en crecer.
        </p>
        <div class="pt-4 relative z-10">
          <a
            href="/contacto"
            class="inline-flex items-center justify-center gap-2 h-14 px-8 text-lg font-semibold rounded-full bg-primary text-white hover:bg-primary/90 transition-all hover:scale-105 shadow-[0_0_20px_rgba(94,23,235,0.3)] hover:shadow-[0_0_35px_rgba(94,23,235,0.5)]"
          >
            Hablar con un experto
            <ArrowRight className="size-5" />
          </a>
        </div>
      </div>
    </div>
  </section>
</PageLayout>