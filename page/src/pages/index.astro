---
import { ContactForm } from "@/components/sections/Contact";
import SocialFollow from "@/components/sections/SocialFollow.astro";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Code2, GraduationCap, Lightbulb } from "lucide-react";
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="BlakIA - Automatización e Inteligencia Artificial para Empresas"
  description="Transformamos tareas repetitivas y procesos manuales en sistemas inteligentes y eficientes 24/7."
>
  <div class="container mx-auto px-4">
    <!-- Hero Section -->
    <section class="relative z-10 text-center pt-20 sm:pt-32 pb-12">
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold tracking-tighter">
        Automatización e Inteligencia Artificial para tu Negocio
      </h1>
      <p class="mt-4 max-w-2xl mx-auto text-lg text-muted-foreground">
        Transformamos tareas repetitivas y procesos manuales en sistemas inteligentes y eficientes 24/7.
      </p>
    </section>
    <section class="relative z-20 text-center pb-20 sm:pb-32">
      <Button size="lg" variant="gradient" asChild>
        <a href="#contact">Cuéntanos tu proyecto</a>
      </Button>
    </section>

    <!-- Services Section -->
    <section id="services" class="py-20 sm:py-24">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold tracking-tight">
          Lo imposible solo tarda un poco más
        </h2>
        <p class="mt-3 max-w-2xl mx-auto text-lg text-muted-foreground">
          No vendemos software. Construimos el futuro que tu negocio necesita, hoy.
        </p>
      </div>

      <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
        <Card
          className="text-center hover:border-primary/80 hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <CardHeader>
            <div
              class="mx-auto bg-primary/10 text-primary rounded-full w-16 h-16 flex items-center justify-center mb-4"
            >
              <Code2 size={32} />
            </div>
            <CardTitle>Desarrollos IA a Medida</CardTitle>
          </CardHeader>
          <CardContent class="text-muted-foreground">
            <p>
              <strong>Si puedes imaginarlo, lo construimos.</strong> Convertimos tus ideas más audaces en herramientas de IA que redefinen tu mercado.
            </p>
          </CardContent>
        </Card>

        <Card
          className="text-center hover:border-primary/80 hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <CardHeader>
            <div
              class="mx-auto bg-primary/10 text-primary rounded-full w-16 h-16 flex items-center justify-center mb-4"
            >
              <Lightbulb size={32} />
            </div>
            <CardTitle>Consultoría IA & Business Intelligence</CardTitle>
          </CardHeader>
          <CardContent class="text-muted-foreground">
            <p>
              <strong>Decisiones, no dudas.</strong> Analizamos tu negocio y te damos el mapa para convertir datos en dominio y estrategia.
            </p>
          </CardContent>
        </Card>

        <Card
          className="text-center hover:border-primary/80 hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <CardHeader>
            <div
              class="mx-auto bg-primary/10 text-primary rounded-full w-16 h-16 flex items-center justify-center mb-4"
            >
              <GraduationCap size={32} />
            </div>
            <CardTitle>Formación para Particulares y Empresas</CardTitle>
          </CardHeader>
          <CardContent class="text-muted-foreground">
            <p>
              <strong>De usuarios a innovadores.</strong> Te damos el conocimiento para que no solo uses la IA, sino que la domines y la crees.
            </p>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- Contact Section -->
    <ContactForm client:load />

    <!-- Social Follow Section -->
    <SocialFollow />
  </div>
</Layout>
